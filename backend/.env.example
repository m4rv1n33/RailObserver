# Copy to backend/.env and fill in. The .env file is gitignored.
# opentransportdata.swiss Train Formation API token (used for EVN-based fleet
# detection in timetable lookup). Leave unset to disable the feature.
OTD_API_TOKEN=
